Temporary bootstrap workflow applies and validates the premium visual system on this branch. It will be removed after the source commit and CI validation.

The trusted workflow copy is temporarily present on the stacked base so pull-request validation can execute with repository write permission.
