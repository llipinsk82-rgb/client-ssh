name: Apply Client SSH 0.3.13

on:
  push:
    branches:
      - agent/0.3.13-background-monitor-self-test
    paths:
      - .github/workflows/agent-apply-0.3.13.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/0.3.13-background-monitor-self-test

      - name: Apply source changes
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def patch(path: str, old: str, new: str) -> None:
              file = Path(path)
              text = file.read_text()
              if old not in text:
                  raise SystemExit(f"Missing patch anchor in {path}: {old[:100]!r}")
              file.write_text(text.replace(old, new, 1))

          patch(
              "app/build.gradle.kts",
              '        versionCode = 55\n        versionName = "0.3.12"',
              '        versionCode = 56\n        versionName = "0.3.13"',
          )

          scheduler = "app/src/main/java/eu/blackserv/clientssh/health/HealthMonitorScheduler.kt"
          patch(
              scheduler,
              "interface HealthWorkScheduler {\n    fun schedule(config: HealthMonitorConfig)\n    fun runNow(profileId: String)\n    fun cancel(profileId: String)\n}",
              "interface HealthWorkScheduler {\n    fun schedule(config: HealthMonitorConfig)\n    fun runNow(profileId: String)\n    fun runDelayedSelfTest(profileId: String, delayMinutes: Long)\n    fun cancel(profileId: String)\n}",
          )
          patch(
              scheduler,
              '''    override fun cancel(profileId: String) {
        require(profileId.isNotBlank()) { "profileId must not be blank" }
        workManager.cancelUniqueWork(uniqueWorkName(profileId))
        workManager.cancelUniqueWork(immediateWorkName(profileId))
    }
''',
              '''    override fun runDelayedSelfTest(profileId: String, delayMinutes: Long) {
        require(profileId.isNotBlank()) { "profileId must not be blank" }
        require(delayMinutes in 1L..60L) { "delayMinutes must be between 1 and 60" }
        val request = OneTimeWorkRequestBuilder<HealthCheckWorker>()
            .setInitialDelay(delayMinutes, TimeUnit.MINUTES)
            .setConstraints(networkConstraints())
            .setInputData(profileInput(profileId, backgroundSelfTest = true))
            .addTag(TAG_ALL_HEALTH_CHECKS)
            .addTag(profileTag(profileId))
            .addTag(TAG_DELAYED_BACKGROUND_SELF_TEST)
            .build()

        workManager.enqueueUniqueWork(
            selfTestWorkName(profileId),
            ExistingWorkPolicy.REPLACE,
            request,
        )
    }

    override fun cancel(profileId: String) {
        require(profileId.isNotBlank()) { "profileId must not be blank" }
        workManager.cancelUniqueWork(uniqueWorkName(profileId))
        workManager.cancelUniqueWork(immediateWorkName(profileId))
        workManager.cancelUniqueWork(selfTestWorkName(profileId))
    }
''',
          )
          patch(
              scheduler,
              '''    private fun profileInput(profileId: String): Data = Data.Builder()
        .putString(HealthCheckWorker.KEY_PROFILE_ID, profileId)
        .build()
''',
              '''    private fun profileInput(
        profileId: String,
        backgroundSelfTest: Boolean = false,
    ): Data = Data.Builder()
        .putString(HealthCheckWorker.KEY_PROFILE_ID, profileId)
        .putBoolean(HealthCheckWorker.KEY_BACKGROUND_SELF_TEST, backgroundSelfTest)
        .build()
''',
          )
          patch(
              scheduler,
              '''        const val TAG_ALL_HEALTH_CHECKS = "health-check-monitor"
        const val TAG_MANUAL_BACKGROUND_TEST = "health-check-manual-background-test"
''',
              '''        const val TAG_ALL_HEALTH_CHECKS = "health-check-monitor"
        const val TAG_MANUAL_BACKGROUND_TEST = "health-check-manual-background-test"
        const val TAG_DELAYED_BACKGROUND_SELF_TEST = "health-check-delayed-background-self-test"
''',
          )
          patch(
              scheduler,
              '''        internal fun immediateWorkName(profileId: String): String =
            "health-check-now-${stableId(profileId)}"

        internal fun profileTag(profileId: String): String =
''',
              '''        internal fun immediateWorkName(profileId: String): String =
            "health-check-now-${stableId(profileId)}"

        internal fun selfTestWorkName(profileId: String): String =
            "health-check-self-test-${stableId(profileId)}"

        internal fun profileTag(profileId: String): String =
''',
          )

          controller = "app/src/main/java/eu/blackserv/clientssh/health/HealthMonitorController.kt"
          patch(
              controller,
              '''    fun removeProfile(profileId: String) {
''',
              '''    fun scheduleBackgroundSelfTest(profileId: String, delayMinutes: Long = 1L) {
        require(profileId.isNotBlank()) { "profileId must not be blank" }
        val config = configRepository.get(profileId)
            ?: throw IllegalStateException("Health monitoring is not configured for this profile")
        check(config.enabled) { "Health monitoring must be enabled before testing background execution" }
        scheduler.runDelayedSelfTest(profileId, delayMinutes)
    }

    fun removeProfile(profileId: String) {
''',
          )

          worker = "app/src/main/java/eu/blackserv/clientssh/health/HealthCheckWorker.kt"
          patch(
              worker,
              '''        val profileId = inputData.getString(KEY_PROFILE_ID)?.takeIf { it.isNotBlank() }
            ?: return Result.failure()
''',
              '''        val profileId = inputData.getString(KEY_PROFILE_ID)?.takeIf { it.isNotBlank() }
            ?: return Result.failure()
        val backgroundSelfTest = inputData.getBoolean(KEY_BACKGROUND_SELF_TEST, false)
''',
          )
          patch(
              worker,
              '''            if (run.transition.notifyStatusChange) {
                HealthStatusNotifier(applicationContext).notifyStatusChange(
                    profileId = profileId,
                    displayName = profile.name.ifBlank { profile.host },
                    snapshot = run.transition.snapshot,
                )
            }

            finish(
                outcome = HealthCheckRunOutcome.SUCCESS,
                detail = run.diagnosticLabel(),
                result = Result.success(),
            )
''',
              '''            val notifier = HealthStatusNotifier(applicationContext)
            if (run.transition.notifyStatusChange) {
                notifier.notifyStatusChange(
                    profileId = profileId,
                    displayName = profile.name.ifBlank { profile.host },
                    snapshot = run.transition.snapshot,
                )
            }
            val selfTestNotificationShown = if (backgroundSelfTest) {
                notifier.notifyBackgroundSelfTest(
                    profileId = profileId,
                    displayName = profile.name.ifBlank { profile.host },
                    snapshot = run.transition.snapshot,
                )
            } else {
                null
            }
            val detail = if (backgroundSelfTest) {
                buildString {
                    append(run.diagnosticLabel())
                    append(" • test tła: ")
                    append(if (selfTestNotificationShown == true) "powiadomienie wysłane" else "powiadomienie zablokowane")
                }
            } else {
                run.diagnosticLabel()
            }

            finish(
                outcome = HealthCheckRunOutcome.SUCCESS,
                detail = detail,
                result = Result.success(),
            )
''',
          )
          patch(
              worker,
              '''    companion object {
        const val KEY_PROFILE_ID = "profile_id"
        internal const val MAX_RETRY_ATTEMPTS = 3
''',
              '''    companion object {
        const val KEY_PROFILE_ID = "profile_id"
        const val KEY_BACKGROUND_SELF_TEST = "background_self_test"
        internal const val MAX_RETRY_ATTEMPTS = 3
''',
          )

          notifier = "app/src/main/java/eu/blackserv/clientssh/health/HealthStatusNotifier.kt"
          patch(
              notifier,
              '''class HealthStatusNotifier(
''',
              '''internal fun healthBackgroundSelfTestContent(
    displayName: String,
    snapshot: HealthCheckSnapshot,
): HealthNotificationContent {
    val target = displayName.trim().ifBlank { "Host" }
    val status = snapshot.status.name
    val latency = snapshot.responseTimeMs?.let { " • TCP ${it} ms" }.orEmpty()
    return HealthNotificationContent(
        title = "Test Monitora w tle zakończony",
        text = "$target: $status$latency. Worker działa po zamknięciu aplikacji.",
    )
}

class HealthStatusNotifier(
''',
          )
          patch(
              notifier,
              '''    private fun notificationsAllowed(): Boolean =
''',
              '''    fun notifyBackgroundSelfTest(
        profileId: String,
        displayName: String,
        snapshot: HealthCheckSnapshot,
    ): Boolean {
        if (!notificationsAllowed()) return false
        createChannel()

        val content = healthBackgroundSelfTestContent(displayName, snapshot)
        val openAppIntent = PendingIntent.getActivity(
            context,
            backgroundSelfTestNotificationId(profileId),
            Intent(context, MainActivity::class.java).apply {
                flags = Intent.FLAG_ACTIVITY_CLEAR_TOP or Intent.FLAG_ACTIVITY_SINGLE_TOP
            },
            PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_IMMUTABLE,
        )
        val notification = NotificationCompat.Builder(context, CHANNEL_ID)
            .setSmallIcon(R.mipmap.ic_launcher)
            .setContentTitle(content.title)
            .setContentText(content.text)
            .setStyle(NotificationCompat.BigTextStyle().bigText(content.text))
            .setContentIntent(openAppIntent)
            .setAutoCancel(true)
            .setCategory(NotificationCompat.CATEGORY_STATUS)
            .build()

        return runCatching {
            NotificationManagerCompat.from(context).notify(
                backgroundSelfTestNotificationId(profileId),
                notification,
            )
            true
        }.getOrDefault(false)
    }

    private fun notificationsAllowed(): Boolean =
''',
          )
          patch(
              notifier,
              '''        internal fun notificationId(profileId: String): Int {
            require(profileId.isNotBlank()) { "profileId must not be blank" }
            return 0x48000000 or (profileId.hashCode() and 0x00ffffff)
        }
''',
              '''        internal fun notificationId(profileId: String): Int {
            require(profileId.isNotBlank()) { "profileId must not be blank" }
            return 0x48000000 or (profileId.hashCode() and 0x00ffffff)
        }

        internal fun backgroundSelfTestNotificationId(profileId: String): Int {
            require(profileId.isNotBlank()) { "profileId must not be blank" }
            return 0x49000000 or (profileId.hashCode() and 0x00ffffff)
        }
''',
          )

          screen = "app/src/main/java/eu/blackserv/clientssh/ui/screens/HealthMonitorScreen.kt"
          patch(
              screen,
              '''    var testingWorkerProfileIds by remember { mutableStateOf(emptySet<String>()) }
''',
              '''    var testingWorkerProfileIds by remember { mutableStateOf(emptySet<String>()) }
    var scheduledSelfTestProfileIds by remember { mutableStateOf(emptySet<String>()) }
''',
          )
          patch(
              screen,
              '''    Scaffold(
''',
              '''    fun scheduleBackgroundSelfTest(profileId: String) {
        if (profileId in scheduledSelfTestProfileIds) return
        controller.scheduleBackgroundSelfTest(
            profileId = profileId,
            delayMinutes = BACKGROUND_SELF_TEST_DELAY_MINUTES,
        )
        scheduledSelfTestProfileIds = scheduledSelfTestProfileIds + profileId
        refresh()
    }

    Scaffold(
''',
          )
          patch(
              screen,
              '''                        testingWorker = profile.id in testingWorkerProfileIds,
                        onSave = ::save,
                        onCheckNow = { checkNow(profile, config) },
                        onTestWorkerNow = { testWorkerNow(profile.id) },
''',
              '''                        testingWorker = profile.id in testingWorkerProfileIds,
                        backgroundSelfTestScheduled = profile.id in scheduledSelfTestProfileIds,
                        onSave = ::save,
                        onCheckNow = { checkNow(profile, config) },
                        onTestWorkerNow = { testWorkerNow(profile.id) },
                        onScheduleBackgroundSelfTest = { scheduleBackgroundSelfTest(profile.id) },
''',
          )
          patch(
              screen,
              '''    testingWorker: Boolean,
    onSave: (HealthMonitorConfig) -> Unit,
    onCheckNow: () -> Unit,
    onTestWorkerNow: () -> Unit,
''',
              '''    testingWorker: Boolean,
    backgroundSelfTestScheduled: Boolean,
    onSave: (HealthMonitorConfig) -> Unit,
    onCheckNow: () -> Unit,
    onTestWorkerNow: () -> Unit,
    onScheduleBackgroundSelfTest: () -> Unit,
''',
          )
          patch(
              screen,
              '''                    testingWorker = testingWorker,
                    now = now,
                    onSave = onSave,
                    onTestWorkerNow = onTestWorkerNow,
''',
              '''                    testingWorker = testingWorker,
                    backgroundSelfTestScheduled = backgroundSelfTestScheduled,
                    now = now,
                    onSave = onSave,
                    onTestWorkerNow = onTestWorkerNow,
                    onScheduleBackgroundSelfTest = onScheduleBackgroundSelfTest,
''',
          )
          patch(
              screen,
              '''    testingWorker: Boolean,
    now: Long,
    onSave: (HealthMonitorConfig) -> Unit,
    onTestWorkerNow: () -> Unit,
    onCopyReport: () -> Unit,
''',
              '''    testingWorker: Boolean,
    backgroundSelfTestScheduled: Boolean,
    now: Long,
    onSave: (HealthMonitorConfig) -> Unit,
    onTestWorkerNow: () -> Unit,
    onScheduleBackgroundSelfTest: () -> Unit,
    onCopyReport: () -> Unit,
''',
          )
          patch(
              screen,
              '''                testing = testingWorker,
                now = now,
                onTestWorkerNow = onTestWorkerNow,
                onCopyReport = onCopyReport,
''',
              '''                testing = testingWorker,
                backgroundSelfTestScheduled = backgroundSelfTestScheduled,
                now = now,
                onTestWorkerNow = onTestWorkerNow,
                onScheduleBackgroundSelfTest = onScheduleBackgroundSelfTest,
                onCopyReport = onCopyReport,
''',
          )
          patch(
              screen,
              '''    testing: Boolean,
    now: Long,
    onTestWorkerNow: () -> Unit,
    onCopyReport: () -> Unit,
''',
              '''    testing: Boolean,
    backgroundSelfTestScheduled: Boolean,
    now: Long,
    onTestWorkerNow: () -> Unit,
    onScheduleBackgroundSelfTest: () -> Unit,
    onCopyReport: () -> Unit,
''',
          )
          patch(
              screen,
              '''            OutlinedButton(onClick = onCopyReport, modifier = Modifier.fillMaxWidth()) {
                Text("Kopiuj raport testowy")
            }
''',
              '''            Text(
                "Pełny test: zaplanuj zadanie, zamknij aplikację normalnie (nie używaj „Wymuś zatrzymanie”). Możesz też zrestartować telefon. Po około minucie powinno pojawić się powiadomienie testowe.",
                style = MaterialTheme.typography.bodySmall,
                color = MonitorTextSecondary,
            )
            OutlinedButton(
                onClick = onScheduleBackgroundSelfTest,
                enabled = enabled && !backgroundSelfTestScheduled,
                modifier = Modifier.fillMaxWidth(),
            ) {
                Text(
                    if (backgroundSelfTestScheduled) {
                        "Test zaplanowany — zamknij aplikację"
                    } else {
                        "Test po zamknięciu aplikacji (1 min)"
                    },
                )
            }
            OutlinedButton(onClick = onCopyReport, modifier = Modifier.fillMaxWidth()) {
                Text("Kopiuj raport testowy")
            }
''',
          )
          patch(
              screen,
              '    appendLine("Client SSH 0.3.5 — Health Monitor")',
              '    appendLine("Client SSH 0.3.13 — Health Monitor")',
          )
          patch(
              screen,
              '''private const val WORKER_TEST_REFRESH_ATTEMPTS = 60
private const val WORKER_TEST_REFRESH_DELAY_MS = 500L
''',
              '''private const val WORKER_TEST_REFRESH_ATTEMPTS = 60
private const val WORKER_TEST_REFRESH_DELAY_MS = 500L
private const val BACKGROUND_SELF_TEST_DELAY_MINUTES = 1L
''',
          )

          controller_test = "app/src/test/java/eu/blackserv/clientssh/health/HealthMonitorControllerTest.kt"
          patch(
              controller_test,
              '''    @Test(expected = IllegalStateException::class)
    fun `testing background worker rejects missing config`() {
''',
              '''    @Test
    fun `delayed background self test requires enabled monitoring and preserves delay`() {
        val fixture = Fixture()
        fixture.controller.save(HealthMonitorConfig(profileId = "a", enabled = true))

        fixture.controller.scheduleBackgroundSelfTest("a", delayMinutes = 3L)

        assertEquals(listOf("a" to 3L), fixture.scheduler.delayedSelfTests)
    }

    @Test(expected = IllegalStateException::class)
    fun `testing background worker rejects missing config`() {
''',
          )
          patch(
              controller_test,
              '''        val runNowRequests = mutableListOf<String>()
        val cancelled = mutableListOf<String>()
''',
              '''        val runNowRequests = mutableListOf<String>()
        val delayedSelfTests = mutableListOf<Pair<String, Long>>()
        val cancelled = mutableListOf<String>()
''',
          )
          patch(
              controller_test,
              '''        override fun cancel(profileId: String) {
''',
              '''        override fun runDelayedSelfTest(profileId: String, delayMinutes: Long) {
            delayedSelfTests += profileId to delayMinutes
        }

        override fun cancel(profileId: String) {
''',
          )

          notifier_test = "app/src/test/java/eu/blackserv/clientssh/health/HealthStatusNotifierTest.kt"
          patch(
              notifier_test,
              '''    @Test
    fun `notification id is stable and profile specific`() {
''',
              '''    @Test
    fun `background self test notification clearly reports worker result`() {
        val content = healthBackgroundSelfTestContent(
            displayName = "Vu+ Zero",
            snapshot = HealthCheckSnapshot(
                profileId = "vuzero",
                status = HealthStatus.ONLINE,
                responseTimeMs = 79,
            ),
        )

        assertEquals("Test Monitora w tle zakończony", content.title)
        assertEquals(
            "Vu+ Zero: ONLINE • TCP 79 ms. Worker działa po zamknięciu aplikacji.",
            content.text,
        )
    }

    @Test
    fun `notification id is stable and profile specific`() {
''',
          )

          Path("docs/NEXT_CHAT_HANDOFF.md").write_text('''# Client SSH — aktualny handoff

Repozytorium: `llipinsk82-rgb/client-ssh`

Aktualnie rozwijana wersja: `0.3.13` (`versionCode 56`).

## Funkcje działające i potwierdzone na telefonie

- profile SSH/Telnet, hasło i klucz prywatny,
- Terminal PTY z Session Keeperem, pełnym logiem i trwałym auto-zawijaniem,
- SFTP na VPS oraz Vu+ Zero/Enigma2,
- jawna weryfikacja fingerprintu i osobne klucze hosta dla każdego `host:port`,
- automatyczna zgodność z nowoczesnym SSH i starym Dropbear,
- Monitor TCP/SSH: CPU, RAM, load, dysk, uptime, sieć i ping,
- skiny Sapphire, Aurora i Obsidian oraz stały splash Sapphire,
- podpisane wydania i aktualizacje OTA,
- szyfrowany eksport/import `.bssh` (AES-256-GCM + PBKDF2), sprawdzony fizycznie.

## Etap 0.3.13

- opóźniony samotest prawdziwego WorkManagera po zamknięciu aplikacji,
- powiadomienie potwierdzające wykonanie testu,
- możliwość sprawdzenia zachowania po restarcie telefonu,
- aktualizacja dokumentacji i domknięcie fizycznej walidacji Issue #20.

## Bezpieczeństwo

- `StrictHostKeyChecking=yes` pozostaje obowiązkowe,
- fingerprinty nie są eksportowane w kopii konfiguracji,
- stały klucz release jest przechowywany wyłącznie w GitHub Actions Secrets,
- Issue #19 pozostaje otwarte: dawny ujawniony materiał podpisujący nadal istnieje w historii Git,
- przepisanie historii i force-push wymagają osobnej, jednoznacznej zgody użytkownika oraz lokalnego zaszyfrowanego backupu mirror.

## Następna kolejność

1. Fizyczny test `0.3.13`: zaplanować test w tle, zamknąć aplikację i odebrać powiadomienie.
2. Powtórzyć test z restartem telefonu.
3. Sprawdzić pojedyncze alerty OFFLINE/ONLINE na profilu testowym.
4. Zamknąć Issue #20 po udokumentowaniu wyników.
5. Przygotować osobne okno serwisowe do oczyszczenia historii Git według `docs/GIT_HISTORY_CLEANUP_PLAN.md`.
6. Następnie rozwijać SFTP Pro, Terminal Pro i Monitor Pro.
''')
          PY

      - name: Commit changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "Client SSH automation"
          git config user.email "actions@users.noreply.github.com"
          git add app docs/NEXT_CHAT_HANDOFF.md
          git commit -m "Add delayed background monitor self-test for 0.3.13"
          git push origin HEAD:agent/0.3.13-background-monitor-self-test
